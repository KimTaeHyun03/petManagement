# 반려동물 헬스케어 — 세부 계획 (PLAN)

> 출처: `RESEARCH.md` (요구사항·사전조사), `plan3.md` (워크플로 정본), `function.md` (담당 정본)
> 핵심 철학: **사진 한 장으로 끝나는 일상 기록 → 타임라인 기반 챗봇 상담**
> 슬로건: *"병원을 최대한 적게 가기 위해 사전에 대비"*

---

## 1. 설계 목표

| 목표 | 설명 | 근거 | 
|---|---|---|
| 사전 예방 | 병원 방문을 줄이기 위한 일상 데이터 기반 조기 경고 | RESEARCH "0. 슬로건" |
| OCR 자동 기록 | 성분표·영수증 사진 한 장으로 입력 부담 제거 | plan3.md 1·2 항 |
| 타임라인 통합 | 체중·음식·접종 기록을 시간순 단일 뷰로 결합 | function.md 김기연 |
| 챗봇 상담 | 누적된 펫 기록을 컨텍스트로 자연어 질의응답 | function.md 김태현 |
| 다묘/다견 관리 | 한 계정에서 여러 반려동물 동시 관리 | RESEARCH "사용 동기" |


---

## 2. 대상 사용자 & 범위

- **종**: 강아지, 고양이 (가장 많이 키우는 두 종에 우선 집중)
- **사용자**: 반려동물 보호자 (1인 1계정, 다수 펫)
- **플랫폼**: 웹 (PC/모바일 반응형) — OpenAI 호출 고려

---

## 3. 시스템 아키텍처

### 3.1 전체 구조도

```
[Client (Web)]
     │
     │ HTTPS / JWT
     ▼
[API Gateway / Backend Server]
     │
     ├──► [도메인 서비스]
     │     ├─ AuthService          (회원가입 / 로그인)
     │     ├─ PetService           (반려동물 등록·프로필·알러지)
     │     ├─ WeightService        (체중 기록·추세·급변 알림)
     │     ├─ OCRService           (이미지 → 텍스트, 영수증/성분표 분류)
     │     ├─ FoodService          (위험음식 검색·성분표 알러지 매칭)
     │     ├─ VaccinationService   (예방접종 일정·이력·영수증 자동 기록)
     │     ├─ TimelineService      (이벤트 통합·대시보드)
     │     └─ ChatbotService       (OpenAI + DB 컨텍스트 RAG)
     │
     ├──► [알림 처리기]
     │     └─ 체중 급변 / 다음 접종일 도래 → 푸시
     │
     └──► [외부 연동]
           ├─ OCR 엔진             (이미지 텍스트 추출)
           ├─ OpenAI API           (챗봇 응답 생성)
           └─ Push           (알림 채널)

[Database]
     ├─ 운영 DB (관계형: User, Pet, Records, Receipts, IngredientScans, ChatLog …)
     └─ 마스터 DB (DangerFood, Vaccine, StandardWeight)
```

### 3.2 사진 업로드 → OCR 자동 분기 *(담당: 장윤서)*

```
[사진 업로드] — 영수증 또는 성분표
        │
        ▼
   [OCRService: 이미지 → 텍스트]
        │
        ▼
   [문서 분류: 영수증 vs 성분표]
   ├─ 영수증 키워드: "결제", "원", "동물병원", 접종 항목명
   └─ 성분표 키워드: "원재료", "성분", "조단백질"
        │
        │   (사용자 확인 UI에서 분류 재확정 가능)
        │
        ├─ 성분표 ─► 4.5 위험 성분 · 알러지 매칭
        └─ 영수증 ─► 4.6 예방접종 자동 기록
```

원본 이미지·텍스트는 `IngredientScan` 또는 `ReceiptRecord`에 저장. **자동 저장이 아닌 사용자 확정 후 저장** — OCR 오인식으로 인한 잘못된 의료 기록 방지.

---

## 4. 기능 설계

### 4.1 회원가입 / 로그인  *(담당: 김태현)*

- 이메일 + 비밀번호 가입, 비밀번호 bcrypt 해시
- JWT(Access) + Refresh Token 인증
- 로그아웃 / 비밀번호 변경

> ⚠ **현 구현 상태(2026-05-18)** — 1차 구현은 **Access-only(httpOnly 쿠키, 기본 2h)** 로 진행되어 본 4.1의 "Refresh Token 인증" 결정값과 어긋난다. 운영 전환 전 Refresh Rotation 도입 + Access TTL 단축이 필요. 또한 비밀번호 해시는 **Argon2id** (research_taehyun.md §1) — 본 절의 "bcrypt" 표기는 후속에 갱신 예정. 상세 트래커: `TODO.md`.

> ※ 집주소 입력은 **제외** — 동물병원 찾기 기능을 1차 범위에서 제외하므로 수집할 이유가 없음 (불필요한 개인정보 최소화)

### 4.2 반려동물 등록  *(담당: 김태현)*

**등록 항목**:
- 기본 정보: 이름, 종(강아지/고양이), 품종, 생년월일, 성별, 중성화 여부
- 초기 건강 정보: 현재 체중(선택), **알러지 목록** (예: 닭고기, 감자, 소고기 …)

**동작**:
- 한 계정에서 다수 등록 가능, 펫 전환 UI 제공
- 등록 후 모든 기록(체중·OCR·접종)은 펫 단위로 분리 저장
- 알러지 정보는 4.5 성분표 매칭에서 사용

### 4.3 체중 관리  *(담당: 김기연)*

**입력**: 날짜(기본값: 입력 시점 날짜, 시간), 몸무게(kg)

**자동 처리**:
- 직전 기록 대비 ±10% 이상 변동 시 "급변" 플래그 → 알림 발송
- 연령·종·품종 표준 체중표(`StandardWeight` 마스터)와 비교 → 정상/과체중/저체중 판정

**알림 정책**:
- 급변(±10% 이상) 즉시 알림
- 정상 추세는 알림 없음

### 4.4 OCR 변환  *(담당: 장윤서)*

**입력**: 사용자가 업로드한 이미지 (영수증 또는 성분표)

**처리 단계**:
1. **이미지 → 텍스트** — OCR 엔진으로 텍스트 추출
2. **문서 분류** — 추출된 텍스트의 키워드 패턴으로 영수증/성분표 구분
   - 영수증: "결제", "원", "동물병원", 접종 항목명 등
   - 성분표: "원재료", "성분", "조단백질" 등
   - 사용자에게 확인 UI 노출
3. **분기 처리**
   - 성분표 → 4.5 위험음식·알러지 매칭으로 전달
   - 영수증 → 4.6 예방접종 자동 기록으로 전달
4. **원본 이미지·텍스트는 `ReceiptRecord` 또는 `IngredientScan`에 저장** — 사용자가 추후 재확인·수정 가능

**오류 처리**:
- 자동 저장이 아닌 "사용자 확정 후 저장" 흐름으로 의료 데이터 오인식 위험 완화

### 4.5 성분표 위험 성분 · 알러지 매칭  *(담당: 장윤서)*

성분표로 분류된 OCR 결과를 두 갈래로 매칭한 뒤 **화면 즉시 경고**를 띄운다 (푸시 알림 채널은 사용하지 않음 — §6).

```
OCR로 추출한 성분 텍스트
   │
   ├─ DangerFood DB 키워드 매칭 (ASPCA 23개)
   │   └─ 일치 시 "이 사료에 [초콜릿] 포함 — 모든 강아지에 위험" 경고
   │
   └─ Pet.allergies 매칭 (해당 펫 알러지)
       └─ 일치 시 "이 펫의 [닭고기] 알러지에 해당" 맞춤 경고
```

매칭 결과는 `IngredientScan.matched_foods_json` / `matched_allergies_json` 에 저장되어 타임라인·챗봇 컨텍스트에서 재참조된다.

**경고 카드 예시**
```
┌────────────────────────────────────────────┐
│ 성분표 분석 결과                            │
├────────────────────────────────────────────┤
│ ⚠ 위험 성분 1건                             │
│  • 양파 분말 — 적혈구 손상 가능 (ASPCA)   │
├────────────────────────────────────────────┤
│ ⚠ [몽이]의 알러지 일치 1건                  │
│  • 닭고기 — 등록된 알러지 항목             │
├────────────────────────────────────────────┤
│ ⚠ 본 정보는 참고용이며 진단이 아닙니다.    │
└────────────────────────────────────────────┘
```

### 4.6 예방접종 관리  *(담당: 김찬영)*

**데이터 모델**:
- 백신 마스터 (`Vaccine`): 종, 차수, 권장 시기(생후 N주), 법적 의무 여부, 위험도
- 접종 이력 (`VaccinationRecord`): 펫별 접종 날짜·백신 종류

**입력 경로**:
1. **수동 입력** — 보호자가 백신 종류·날짜 직접 선택
2. **영수증 OCR 자동 기록** — 동물병원 영수증 업로드

**알림 로직** (RESEARCH "3. 예방접종" 인용):
- 접종 완료 입력 → 다음 권장일 자동 계산 → 알림 예약
- 반려동물들은 **6차까지** 맞는 접종도 있음 (다회차 처리)
- 예: 심장사상충 — 정기 투약 알림

**위험도 계층 분리** (단순 시각 분류):
- **법적 의무(광견병)**: 미접종 시 강조 경고 + 처벌 안내
- **고위험 백신** (치사율·위험도 높은 질병)
- **저위험 백신** — 시각적 분리

### 4.7 통합 타임라인 & 대시보드  *(담당: 김기연)*

모든 이벤트를 시간축 단일 뷰로 결합:

```
3/01  💉 예방접종: 종합백신 완료
3/15  ⚖  몸무게: 4.2kg (정상)
3/18  📷 성분표 OCR: "양파 분말" 위험 성분 감지
3/22  ⚖  몸무게: 3.9kg (-0.3kg, 급변 알림)
3/25  💉 영수증 OCR: 광견병 접종 자동 기록
```


### 4.8 챗봇  *(담당: 김태현)*

**구현 방식**: OpenAI API + DB 컨텍스트 (RAG식)

**입력 흐름**:
```
사용자 질문 (자연어)
   │
   ▼
[ChatbotService]
   ├─ 해당 펫의 최근 N개 타임라인 이벤트 조회
   ├─ Pet 프로필(종·품종·체중·알러지) 조회
   ├─ 시스템 프롬프트 + 컨텍스트 + 사용자 질문 조립
   ▼
OpenAI API 호출 → 자연어 응답
   │
   ▼
ChatLog 저장 + 사용자에게 노출
```

**제약**:
- AI 진단 금지 — 시스템 프롬프트에 "진단·처방 X, 일반 정보 제공만" 명시
- 면책 고지 항상 노출 ("응급 시 동물병원 방문 권장")
- 응답 컨텍스트는 펫 단위로 격리 (다른 사용자 데이터 누출 차단)

**데이터 모델**:
- `ChatLog (id, user_id, pet_id, role, message, context_snapshot_json, created_at)`
- `context_snapshot_json`: 응답 시점에 사용된 타임라인/펫 정보 요약 (감사·재현용)

---

## 5. 데이터베이스 설계 (ERD 개요)

```
User(사용자 정보) (id, email, pw_hash, created_at)
  │
  └── Pet(펫 정보) (id, user_id, name, species, breed, birth, gender, neutered, allergies_json)
         │
         ├── WeightRecord(몸무게)       (id, pet_id, weight, recorded_at, memo)
         ├── VaccinationRecord(예방접종)  (id, pet_id, vaccine_id, dose_no, vaccinated_at, source: manual|ocr)
         ├── ReceiptRecord(먹은 음식)      (id, pet_id, image_url, extracted_text, parsed_json, paid_at, created_at)
         ├── IngredientScan(영수증 이미지)    (id, pet_id, image_url, extracted_text, matched_foods_json, matched_allergies_json, scanned_at)
         └── ChatLog(채팅내역)            (id, user_id, pet_id, role, message, context_snapshot_json, created_at)

DangerFood(위험음식)     (id, name, aspca_ref, toxic_compound, symptoms, threshold_per_kg, severity, source_url) ※ 마스터
Vaccine(백신)        (id, species, name, recommend_weeks, dose_total, mandatory, severity)                    ※ 마스터
StandardWeight(강아지별 정상 체중) (id, species, breed, age_range, min_kg, max_kg)                                          ※ 마스터
```

**제약/인덱스 메모**:
- `Pet.user_id` 인덱스 (펫 목록 조회 빈번)
- `WeightRecord (pet_id, recorded_at DESC)` 복합 인덱스 (추세 조회)
- `VaccinationRecord (pet_id, vaccinated_at DESC)` 복합 인덱스 (다음 접종일 계산)
- `IngredientScan.scanned_at` 인덱스 (타임라인 최근 조회)
- `ChatLog (pet_id, created_at DESC)` 복합 인덱스 (대화 이력)
- 사용자-펫 권한 검증은 모든 API에서 강제

---

## 6. 알림 정책

위험도 점수 엔진은 MVP 범위에서 제외하되, 단순 규칙 기반 알림은 유지한다.

| 트리거 | 조건 | 채널 |
|---|---|---|
| 체중 급변 | 직전 기록 대비 ±10% 이상 변동 | Push / Email |
| 다음 접종일 도래 | 권장일 D-7, D-1, D-day | Push / Email |
| 미접종 의무 백신 | 광견병 등 권장일 초과 | Push (강조 톤) |
| 성분표 위험 성분 감지 | OCR 매칭 결과 위험 항목 ≥1 | 화면 즉시 경고 (알림 채널 X) |


---

## 7. 외부 API & 데이터 출처

| 용도 | 1차 소스 (Primary) | 교차 검증 (Secondary) |
|---|---|---|
| 위험 음식 기준 | **ASPCA Animal Poison Control** | Merck Veterinary Manual, VCA Hospitals |
| 강아지 백신 일정 | **KASA** (한국수의사회), **농림축산검역본부** | WSAVA Vaccination Guidelines |
| 고양이 백신 일정 | **KASA**, **AVMA** | AAFP Feline Vaccination Advisory Panel |
| 광견병 법적 의무 | 농림축산검역본부 (가축전염병예방법) | 환경부 / 지자체 고시 |
| 표준 체중표 | KASA, AVMA | 사료사 공개 데이터 (Royal Canin, Hill's) — 보조 |
| OCR 엔진 | (선정 예정 — Google Vision / Tesseract / Naver Clova OCR 비교 후 결정) | — |
| 챗봇 응답 생성 | **OpenAI API** | 컨텍스트는 자체 DB에서만 주입 |

### 7.1 의료 자료 정확성 검증 절차

기능 특성상 **오정보가 곧 반려동물 건강 위험**으로 직결되므로 다음 절차를 따른다.

1. **1차 출처 우선** — 학회·정부 기관 자료를 먼저 채택, 블로그·요약 글은 인용 금지
2. **2개 이상 교차 검증** — 위험 음식·백신 항목은 1차/2차 소스 모두에서 일치하는 것만 시드 데이터에 등재
3. **수의사 검수 (가능 시)** — 발표 전 1회 도메인 전문가 리뷰
4. **OpenAI 응답은 의료 결정 근거로 단독 사용 금지** — 챗봇 응답에 "AI 추정" 라벨 + 1차 소스 참고 안내 함께 노출
5. **면책 고지** — UI 푸터·검색 결과·챗봇 응답에 "본 정보는 참고용이며 응급 시 동물병원 방문 권장" 문구 상시 노출
6. **OCR 결과 사용자 확정 단계** — 자동 저장이 아닌 사용자 확정 후 저장 (오인식으로 인한 잘못된 의료 기록 방지)

---

## 8. 화면 흐름 (주요)

```
[로그인 / 회원가입]
   │
[홈 / 펫 선택]
   │   └─ [반려동물 등록]
   │
   ├─ [펫 대시보드]
   │     ├─ 다음 접종 일정
   │     ├─ 체중 차트
   │     ├─ 최근 OCR 경고
   │     └─ 통합 타임라인
   │
   ├─ [체중 입력]
   │
   ├─ [OCR 업로드]
   │     ├─ 성분표 결과 → 위험 성분 & 알러지 매칭
   │     └─ 영수증 결과 → 접종 자동 기록 (사용자 확정)
   │
   ├─ [예방접종 관리]
   └─ [챗봇]
```

---

## 9. 비기능 요구사항

| 항목 | 목표 |
|---|---|
| 응답시간 | 일반 API 300ms 이내, OCR 5초 이내, 챗봇 응답 10초 이내 |
| 보안 | JWT 인증, HTTPS, 비밀번호 해시(bcrypt), 펫 격리 권한 검증 |
| 가용성 | OpenAI·OCR 장애 시 캐시·폴백 메시지 |
| 언어 | 한국어 단일 (다국어 미지원) |

---

## 10. 기술 스택

**Frontend**
- React, TypeScript, CSS-in-JS
- ⚠ CSS 파일은 `.tsx`에 합치지 말고 **별도 파일로 분리** (스타일 책임 분리)

**Backend**
- Node.js (Express 기반 REST API)

**Database**
- AWS RDS (관계형, PostgreSQL)

**외부 API**
- OCR 엔진 (선정 예정)
- OpenAI API (챗봇)

**배포**
- React 빌드 → S3 정적 업로드 (또는 EC2로 한번에)
- Node.js → EC2 업로드
- DB → RDS
- 이미지 저장소 → S3 (OCR 원본 보관)

---

## 11. 개발 단계 (소프트웨어 생명주기 매핑)

| 단계 | 산출물 | 본 PLAN과의 관계 |
|---|---|---|
| 1. 타당성 조사 | 슬로건·요구사항 | RESEARCH.md 완료 |
| 2. 요구사항 분석 | 기능 목록 | RESEARCH.md + function.md |
| 3. **설계** | **본 PLAN.md** | ← **현재 단계** |
| 4. 개발 | 코드, 시드 데이터 | TODO.md 주차별 일정 |
| 5. 테스트 | OCR 정확도 검증, 챗봇 컨텍스트 격리 검증 | 9~10주차 |
| 6. 배포 | S3/EC2/RDS 배포 | 12주차 |
| 7. 운영 | 알림·OCR·챗봇 캐시 모니터링 | 운영 단계 |

---