# 반려동물 헬스케어 — 세부 계획 (PLAN)

> 출처: `RESEARCH.md` (요구사항·사전조사)
> 핵심 철학: **분리된 기능 → 반려동물(Pet) 중심의 유기적 시스템**
> 슬로건: *"병원을 최대한 적게 가기 위해 사전에 대비"*

---

## 1. 설계 목표

| 목표 | 설명 | RESEARCH 근거 |
|---|---|---|
| 사전 예방 | 병원 방문을 줄이기 위한 일상 데이터 기반 조기 경고 | "0. 슬로건" |
| 데이터 연계 | 체중·위험음식·예방접종이 서로 영향을 주는 단일 도메인 | "4. 데이터간의 관계성" |
| 자동 트리거 | 사용자 입력이 아닌 데이터 변화로 분석이 자동 실행 | "시스템 구조도" |
| 다묘/다견 관리 | 한 계정에서 여러 반려동물 동시 관리 | "사용 동기" |

> ※ 사용 동기 부여(포인트 보상)는 **보류** — RESEARCH에서 검토되었으나 사용처 설계 부담으로 1차 범위에서 제외

---

## 2. 대상 사용자 & 범위

- **종**: 강아지, 고양이 (가장 많이 키우는 두 종에 우선 집중)
- **사용자**: 반려동물 보호자 (1인 1계정, 다수 펫)
- **플랫폼**: 웹 (PC/모바일 반응형) — Gemini 호출 고려

---

## 3. 시스템 아키텍처

### 3.1 전체 구조도

```
[Client (Web)]
     │
     │ HTTPS / JWT
     ▼
[API Gateway / Backend Server]
     │
     ├──► [도메인 서비스]
     │     ├─ AuthService         (회원가입 / 로그인)
     │     ├─ PetService          (반려동물 등록·프로필)
     │     ├─ WeightService       (체중 기록·추세)
     │     ├─ FoodService         (위험음식 검색·섭취 기록)
     │     ├─ VaccinationService  (예방접종 일정·이력)
     │     └─ SymptomService      (증상 입력 수집·종합)
     │
     ├──► [이벤트 처리기 (Event Bus)]
     │     └─ 데이터 변경 → 위험도 재계산 → 알림 발송
     │
     ├──► [위험도 점수 엔진 (Risk Engine)]
     │
     └──► [외부 연동]
           ├─ Gemini API          (음식 AI 보조 검색)
           └─ Push/Email          (알림 채널)

[Database]
     ├─ 운영 DB (관계형: User, Pet, Records …)
     └─ 위험음식 마스터 DB (ASPCA 기반 23개 + 확장)
```

### 3.2 핵심 흐름 — "이벤트 기반 자동 재분석"

```
사용자 입력 (체중/음식/접종/증상)
        │
        ▼
[도메인 서비스 저장]
        │
        ▼
[이벤트 발행]  ──► [위험도 재계산]
        │              │
        │              ├─ 최근 위험음식 이력
        │              ├─ 체중 변화 추세
        │              ├─ 미접종 백신 여부
        │              └─ 증상 입력 이력
        │              ▼
        │         [종합 위험도 점수]
        │              │
        │              ▼
        └────────► [알림/타임라인 갱신]
```

### 3.3 단순 조합 vs 유기적 시스템 (RESEARCH 인용)

| 항목 | 단순 조합 방식 | **채택: 유기적 시스템** |
|---|---|---|
| 트리거 | 사용자가 증상 입력할 때만 | 데이터 변화 시 자동 |
| 관계 | 독립된 기능 | 서로 영향을 주는 단일 시스템 |
| 알림 | 없음 | 위험도 점수 기반 능동 알림 |
| 분석 | 현재 시점만 | 시간 흐름에 따른 패턴 감지 |

---

## 4. 기능 설계

### 4.1 회원가입 / 로그인

- 이메일 + 비밀번호 가입, 비밀번호 bcrypt 해시
- JWT(Access) + Refresh Token 인증
- 로그아웃 / 비밀번호 변경

> ※ 집주소 입력은 **제외** — 동물병원 찾기 기능을 1차 범위에서 제외하므로 수집할 이유가 없음 (불필요한 개인정보 최소화)


### 4.2 반려동물 등록

- 등록 항목: 이름, 종(강아지/고양이), 품종, 생년월일, 성별, 중성화 여부, 현재 체중(선택)
- 한 계정에서 다수 등록 가능, 펫 전환 UI 제공
- 등록 후 모든 기록(체중·음식·접종·증상)은 펫 단위로 분리 저장

### 4.3 체중 관리

**입력**: 날짜(기본값: 오늘 자동 선택, 필요 시 변경 가능), 몸무게(kg), (선택) 메모

**자동 처리**:
- 평균 대비 ±10% 이상 변동 시 "급변" 플래그
- 연령·종·품종 표준 체중표와 비교 → 정상/과체중/저체중
- 결과 화면은 **성격유형 검사 결과창 스타일** (RESEARCH "1. 성격유형 검사지" 명시)
  - 정상: 간단한 코멘트
  - 과/저체중: 발생 가능 질병(예: 관절염) 경고 카드 — 경고 톤

**모니터링 주기** (이벤트 기반):
- 기본: 주 1회 알림
- 위험음식 섭취 직후: 3일 1회로 단축 (RESEARCH "4. 데이터간의 관계성" 인용)

### 4.4 위험 음식 검색

**검색 흐름**:
```
사용자 입력 음식명
   ├─ DB 매칭 (ASPCA 기반 23개) → 즉시 결과
   └─ DB 미존재 → Gemini API 질의 → 결과 캐시 저장
```

**위험도 계산** *(공식·계수는 자료 검증 후 확정)*:
- 음식 자체 독성 등급 × 섭취량(g) × 반려동물 체중(kg)
- 예: **초콜릿 — 카카오 함량(다크/밀크)과 섭취량으로 위험도 분기** (RESEARCH 명시)
- 결과: `안전 / 주의 / 위험 / 응급` 4단계

**섭취 기록 저장 → 이벤트 발행** → 체중 모니터링 주기 단축

### 4.5 예방접종 관리

**데이터 모델**:
- 백신 마스터: 종(강아지/고양이), 차수, 권장 시기(생후 N주), 법적 의무 여부, 위험도
- 접종 이력: 펫별 접종 날짜·백신 종류

**알림 로직** (RESEARCH "3. 예방접종" 인용):
- 보호자가 "오늘 접종 완료" 입력 → 다음 권장일 자동 계산 → 알림 예약
- 애기들은 **6차까지** 맞는 접종도 있음 (다회차 처리)
- 예: 심장사상충 — 정기 투약 알림

**위험도 계층 분리**:
- **법적 의무(광견병)**: 미접종 시 강조 경고 + 처벌 안내
- **고위험 백신** (치사율·위험도 높은 질병)
- **저위험 백신** — 시각적 분리

### 4.6 증상 입력 (수집·종합)

**입력 UI**
- 체크박스 UI로 증상 다중 선택 (구토, 설사, 무기력 …)
- **자유 입력란(text)** — 체크박스에 없는 증상을 보호자가 직접 서술
  - 길이 제한: 500자 이내
  - 위험도 엔진은 우선 체크박스 항목만 사용, 자유 입력은 이력·타임라인 노출용
  - (선택 확장) 자유 입력 텍스트에서 키워드 매칭으로 미등록 증상 후보를 모아 마스터 항목으로 승격하는 운영 프로세스

**저장·분석 정책**
- **AI 진단 없음** — 의료 진단 영역(증상 → 질환 매핑)은 본 시스템 범위 밖 (책임 회피)
- 펫별 증상 이력 누적·조회

**입력 후 결과 피드백 (A + B + C 조합 — 13.3 결정 사항)**

| # | 동작 | 비고 |
|---|---|---|
| **A** | 위험도 점수 즉시 갱신 | 증상 저장 → Event Bus 이벤트 발행 → 6절 Risk Engine 재계산 → 등급 카드 갱신 |
| **B** | 관련 이벤트 묶음 카드 | 같은 펫의 **최근 7일** 음식·체중·접종 이벤트를 "관련 정황" 카드로 자동 묶음 (시간 근접만, 인과 추정 X) |
| **C** | 정적 권고 문구 | 증상별 일반 안내 매핑 (예: "구토 24시간 이상 지속 시 동물병원 방문 권장"). **진단 아닌 가이드 문구**, 7.1 면책 고지 동반 |

**A·B·C 결과 카드 예시**
```
┌────────────────────────────────────────────┐
│ 증상 기록 완료 — 위험도: 관찰 → 주의 (↑)  │  ← A
├────────────────────────────────────────────┤
│ 관련될 수 있는 최근 정황                   │  ← B
│  • 3/20 위험음식: 포도 소량 섭취          │
│  • 3/22 몸무게: -0.3kg                    │
├────────────────────────────────────────────┤
│ 일반 가이드                                │  ← C
│  • 구토가 24시간 이상 지속되면            │
│    동물병원 방문을 권장합니다.            │
│ ⚠ 본 정보는 참고용이며 진단이 아닙니다.    │
└────────────────────────────────────────────┘
```

**C 매핑 데이터 (마스터)**
- `SymptomGuide (id, symptom_code, message, severity, source_url, source_year)` 테이블 신설
- 7.1 검증 절차 적용 — 1차 출처(KASA·AVMA·Merck) 기반, 수의사 검수 권장
- 운영 중 추가/수정 가능하도록 마스터 데이터로 분리

### 4.7 통합 타임라인 (Cross-cutting)

RESEARCH 시나리오를 그대로 화면에 매핑:

```
3/01  💉 예방접종: 종합백신 완료
3/15  ⚖  몸무게: 4.2kg (정상)
3/20  ⚠  위험음식: 포도 소량 섭취
3/22  ⚖  몸무게: 3.9kg (-0.3kg)
3/23  🔔 시스템: "포도 섭취 후 체중 감소, 증상 확인 권장"
```

- 모든 이벤트를 시간축 단일 뷰로 표시
- 인과 의심 라인을 시각적으로 연결 (예: 음식 섭취 → 체중 감소)

---

## 5. 데이터베이스 설계 (ERD 개요)

```
User (id, email, pw_hash, created_at)
  │
  └── Pet (id, user_id, name, species, breed, birth, gender, neutered)
         │
         ├── WeightRecord       (id, pet_id, weight, recorded_at, memo)
         ├── FoodIntake         (id, pet_id, food_id, amount_g, eaten_at)
         ├── VaccinationRecord  (id, pet_id, vaccine_id, dose_no, vaccinated_at)
         ├── SymptomLog         (id, pet_id, symptoms_json, free_text, logged_at)
         └── RiskScore          (id, pet_id, score, computed_at, breakdown_json)

DangerFood     (id, name, aspca_ref, base_toxicity, notes)                          ※ 마스터
Vaccine        (id, species, name, recommend_weeks, dose_total, mandatory, severity) ※ 마스터
StandardWeight (id, species, breed, age_range, min_kg, max_kg)                       ※ 마스터
SymptomGuide   (id, symptom_code, message, severity, source_url, source_year)        ※ 마스터 — 4.6 C용
```

**제약/인덱스 메모**:
- `Pet.user_id` 인덱스 (펫 목록 조회 빈번)
- `WeightRecord (pet_id, recorded_at DESC)` 복합 인덱스 (추세 조회)
- `FoodIntake.eaten_at` 인덱스 (최근 7일 조회)
- 사용자-펫 권한 검증은 모든 API에서 강제

---

## 6. 위험도 점수 엔진 설계

**입력 요소** (가중치 초안, 튜닝 필요):

| 요소 | 가중치 | 산출 방식 |
|---|---|---|
| 최근 7일 위험음식 섭취 | 0.35 | 위험도 합산 (4단계 점수화) |
| 체중 급변(±10% 이상) | 0.25 | 표준 체중 대비 편차 |
| 미접종 의무 백신 | 0.20 | 광견병 등 법적 의무 우선 |
| 증상 입력 강도 | 0.20 | 다중 증상 가중 합 |

- 총점 0~100, 등급: `안심(0-30) / 관찰(31-60) / 주의(61-80) / 위험(81-100)`
- **등급 변경 시** 푸시 알림 발송 (변경 없으면 알림 X)
- 저장 → 타임라인에 기록

**튜닝 방안**:
- 가중치는 환경변수/DB 설정값으로 외부화 → 코드 수정 없이 조정
- 더미 시나리오 (포도 섭취 → 체중 감소 → 등급 상승)로 검증

---

## 7. 외부 API & 데이터 출처 (RESEARCH "조사 대상" 인용)

| 용도 | 1차 소스 (Primary) | 교차 검증 (Secondary) |
|---|---|---|
| 위험 음식 기준 | **ASPCA Animal Poison Control** | Merck Veterinary Manual, VCA Hospitals |
| 위험도 공식 (독성 등급·체중 환산) | 수의독성학 교과서 / 동료심사 논문 | ASPCA APCC 사례 데이터 |
| 강아지 백신 일정 | **KASA** (한국수의사회), **농림축산검역본부** | WSAVA Vaccination Guidelines |
| 고양이 백신 일정 | **KASA**, **AVMA** | AAFP Feline Vaccination Advisory Panel |
| 광견병 법적 의무 | 농림축산검역본부 (가축전염병예방법) | 환경부 / 지자체 고시 |
| 표준 체중표 | KASA, AVMA | 사료사 공개 데이터 (Royal Canin, Hill's) — 보조 |
| AI 보조 검색 (DB 미존재 음식) | Google **Gemini API** | (AI 응답은 항상 1차 소스 대조 후 캐시) |

### 7.1 의료 자료 정확성 검증 절차

기능 특성상 **오정보가 곧 반려동물 건강 위험**으로 직결되므로 다음 절차를 따른다.

1. **1차 출처 우선** — 학회·정부 기관 자료를 먼저 채택, 블로그·요약 글은 인용 금지
2. **2개 이상 교차 검증** — 위험 음식·백신 항목은 1차/2차 소스 모두에서 일치하는 것만 시드 데이터에 등재
3. **수의사 검수 (가능 시)** — 발표 전 1회 도메인 전문가 리뷰
4. **Gemini 응답은 의료 결정 근거로 단독 사용 금지** — DB 미존재 음식에 한정, 응답에 "AI 추정" 라벨 + 1차 소스 참고 안내 함께 노출
5. **면책 고지** — UI 푸터·검색 결과에 "본 정보는 참고용이며 응급 시 동물병원 방문 권장" 문구 상시 노출

---

## 8. 화면 흐름 (주요)

```
[로그인 / 회원가입]
   │
[홈 / 펫 선택]
   │   └─ [반려동물 등록]
   │
   ├─ [펫 대시보드]
   │     ├─ 위험도 점수 카드
   │     ├─ 체중 차트
   │     ├─ 다음 접종 일정
   │     └─ 통합 타임라인
   │
   ├─ [위험 음식 검색]
   ├─ [증상 입력]
   ├─ [예방접종 관리]
   └─ [마이페이지]
```

---

## 9. 비기능 요구사항

| 항목 | 목표 |
|---|---|
| 응답시간 | 일반 API 300ms 이내, AI 호출 5초 이내 |
| 보안 | JWT 인증, HTTPS, 비밀번호 해시(bcrypt) |
| 가용성 | Gemini 장애 시 캐시·폴백 메시지 |
| 언어 | 한국어 단일 (다국어 미지원) |

---

## 10. 기술 스택

**Frontend**
- React, TypeScript, CSS-in-JS
- ⚠ CSS 파일은 `.tsx`에 합치지 말고 **별도 파일로 분리** (스타일 책임 분리)

**Backend**
- Node.js (Express 기반 REST API)

**Database**
- AWS RDS (관계형, PostgreSQL 또는 MySQL 중 RDS 비용·운영 편의로 결정)

**배포**
- React 빌드 → S3 정적 업로드 (또는 EC2로 한번에)
- Node.js → EC2 업로드
- DB → RDS

---

## 11. 개발 단계 (소프트웨어 생명주기 매핑)

RESEARCH의 "1. 타당성 조사 → 7. 운영 및 유지 관리" 흐름을 다음 페이즈로 매핑:

| 단계 | 산출물 | 본 PLAN과의 관계 |
|---|---|---|
| 1. 타당성 조사 | 슬로건·요구사항 | RESEARCH.md 완료 |
| 2. 요구사항 분석 | 기능 목록 | RESEARCH.md 2~2-1절 |
| 3. **설계** | **본 PLAN.md** | ← **현재 단계** |
| 4. 개발 | 코드, 시드 데이터 | TODO.md 주차별 일정 |
| 5. 테스트 | 통합 시나리오, 위험도 검증 | 9~10주차 |
| 6. 배포 | S3/EC2/RDS 배포 | 12주차 |
| 7. 운영 | 알림·캐시 모니터링 | 운영 단계 |

---

## 12. 다음 단계 (개발 페이즈로 넘어가기 전 확인)

| # | 항목 | 담당 / 시점 |
|---|---|---|
| 1 | 기술 스택 확정 | ✅ 10절 명시 완료 |
| 2 | ERD 상세화 (제약조건·인덱스) | 자료조사 직후 |
| 3 | API 명세서(REST) 작성 | 기능 확정 후 초안 작성 |
| 4 | 위험도 가중치 검증용 더미 시나리오 | 구현 단계 |
| 5 | 위험 음식 위험도 공식 자료 검증 | 자료조사 분담자 |
| 6 | Gemini 프롬프트 템플릿 초안 | 위험음식 검색 담당자 |
| 7 | 화면 와이어프레임 (Figma) | 모든 기능 구현 후 디자인 단계 |

> 📌 주차별 일정과 역할 분담은 **TODO.md** 참조

---

## 13. 범위 정리 — 보류 / 제외

### 13.1 보류 (MVP 이후 재검토)
- **포인트 보상 시스템** — 사용 동기 부여용으로 RESEARCH에서 검토되었으나, 포인트 사용처 설계 부담이 커서 MVP 이후로 미룸
- **증상 → AI 진단** — 본 MVP는 진단 X. 결과 피드백은 4.6 / 13.3의 **A+B+C 조합**으로 확정 (위험도 갱신 + 관련 이벤트 묶음 + 정적 권고 문구)

### 13.2 제외 (개발 안 함)
- **동물병원·보호소 찾기** — 네이버 지도 연동 부담, 집주소 수집 회피 차원에서 1차 범위 밖
- **다국어 확장** — 한국어 단일 운영. 음식명 영문 동의어 매칭도 제외

### 13.3 증상 입력 결과 피드백 (논의 필요)

> 보호자가 증상을 입력했는데 화면에 아무 반응이 없으면 입력 동기가 떨어진다는 우려에 대한 설계 옵션.

**원칙**: AI 진단은 안 함 (의료 책임 회피). 단, 정적·결정적 피드백은 노출 가능.

| 옵션 | 내용 | 의료 면책 부담 | 구현 난이도 |
|---|---|---|---|
| A. 위험도 점수 즉시 갱신 | 증상 입력 → 6절 Risk Engine 즉시 재계산 → 등급 카드 갱신 | 낮음 (이미 설계됨) | 낮음 |
| B. 관련 이벤트 묶음 표시 | 같은 펫의 최근 7일 음식·체중·접종 이벤트를 "관련 정황" 카드로 자동 묶음 (인과 추정 X, 시간 근접만) | 낮음 | 중간 |
| C. 증상→권고 정적 매핑 | 증상별 일반 권고 메시지 매핑 (예: "구토 24시간 지속 시 병원 방문 권장"). 진단 아닌 가이드 문구 | 중간 (문구 검수 필요) | 중간 |
| D. 증상→가능 질환 매핑 | 증상 조합으로 후보 질환 노출 | **높음** (의료 진단 영역) | 높음 |

**확정: A + B + C 조합** ✅
- A로 즉각 피드백 (등급 등 시각 변화) — 사용 동기 부여
- B로 RESEARCH 핵심인 "유기적 시스템·통합 타임라인" 실체 강화
- C는 7.1 면책 고지 + 1차 출처 명시한 짧은 안내 문구만 (예: 지속시간·심각도 기반 일반 가이드)
- D는 명백한 의료 진단 영역이라 **제외**

→ 4.6절에 정책 반영 완료. `SymptomGuide` 마스터 테이블 신설(5절 ERD).