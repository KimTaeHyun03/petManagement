# 사용자 워크플로우 (반려동물 헬스케어)

> 출처: `PLAN.md` (시스템 설계) + `research_taehyun.md` (인증) + `research_chanyoung.md` (백신) + `function.md` (분담)
> 시점: 2026-05-04 / MVP 1차 범위 기준

---

## 0. 전체 사용 시나리오 (Bird's-eye view)

```
[가입·로그인] → [반려동물 등록] → [일상 데이터 입력]
                                         │
                                         ▼
                              ┌──────────────────────┐
                              │ 이벤트 발행            │
                              │ (체중/음식/접종/증상)  │
                              └────────┬─────────────┘
                                       ▼
                              ┌──────────────────────┐
                              │ 위험도 재계산          │
                              │ (Risk Engine)        │
                              └────────┬─────────────┘
                                       ▼
                       [등급 변경 시 알림] → [대시보드 / 타임라인]
```

핵심: **사용자가 "분석" 버튼을 누르는 게 아니라**, 데이터 입력만으로 자동 재분석이 도는 *유기적 시스템*.

---

## 1. 가입 → 첫 로그인 (담당: 김태현)

```
[랜딩 화면]
   │
   ├─ "회원가입"
   │     ├─ 이메일 입력 (RFC 5322 검증, 소문자 정규화)
   │     ├─ 비밀번호 입력 (최소 10자, 차단 목록 + Pwned Passwords 검사)
   │     ├─ 중복 검사
   │     ├─ Argon2id 해시 → DB 저장
   │     └─ 자동 로그인 X → 로그인 화면으로 이동
   │
   └─ "로그인"
         ├─ 이메일 + 비밀번호 입력
         ├─ 사용자 없어도 더미 해시 비교 (타이밍 공격 방어)
         ├─ 검증 성공 → Access(15분) + Refresh(14일) 발급
         └─ httpOnly + Secure + SameSite=Strict 쿠키로 전달
              │
              ▼
         [홈 / 펫 선택]
```

**비밀번호 분실 시**
1. "비밀번호 찾기" → 이메일 입력 (응답은 가입 여부와 무관하게 동일)
2. CSPRNG 토큰 메일 발송 (DB에는 SHA-256 해시만, 30분 1회용)
3. 새 비밀번호 입력 → 모든 활성 세션·Refresh 무효화 → 완료 알림 메일

---

## 2. 반려동물 등록 (담당: 김태현)

```
[홈 / 펫 선택] ── 처음이면 자동으로 등록 화면
   │
   ▼
[반려동물 등록]
   ├─ 이름 (필수)
   ├─ 종 (강아지/고양이)
   ├─ 품종 (자유 입력 + 자동완성)
   ├─ 생년월일 (모르면 "대략 N개월" 토글)
   ├─ 성별 / 중성화 여부
   └─ 현재 체중 (선택)
         │
         ▼
   [펫 대시보드로 이동]
```

- 한 계정에 **다수 펫** 등록 가능, 헤더 드롭다운으로 전환
- 등록 후 모든 기록은 `pet_id`로 분리 저장, 모든 API에서 사용자-펫 권한 검증 강제

---

## 3. 일상 데이터 입력 → 자동 분석 (핵심 루프)

### 3.1 체중 입력 (담당: 김기연)

```
[체중 입력] ── 날짜 + 몸무게(kg) + (선택)메모
        │
        ▼
   저장 → 이벤트 발행
        │
        ▼
   ┌────────────────────────────────────┐
   │ 자동 처리                          │
   │  • 평균 대비 ±10% 이상 → 급변 플래그│
   │  • 표준 체중표 비교 → 정상/과/저체중│
   └─────────────┬──────────────────────┘
                 ▼
         [성격유형 결과창 스타일 화면]
            정상: 간단 코멘트
            과/저체중: 관절염 등 경고 카드
                 │
                 ▼
         Risk Engine 재계산 (3.5절)
```

**모니터링 주기**: 기본 주 1회 알림 → 위험음식 섭취 직후 **3일 1회로 단축**

### 3.2 위험 음식 검색·기록 (담당: 장윤서)

```
[음식 이름 입력]
        │
        ├─ DB 매칭 (ASPCA 23개) → 즉시 결과
        │
        └─ DB 미존재 → Gemini API 질의
                       └─ 응답 캐시 + "AI 추정" 라벨 + 1차 소스 안내
        │
        ▼
   결과: 안전 / 주의 / 위험 / 응급 (4단계)
        │
        ▼
   [섭취 기록] (음식 + 섭취량g + 시각)
        │
        ▼
   이벤트 발행 → 체중 모니터링 주기 단축 + Risk Engine 재계산
```

위험도 = **음식 독성 등급 × 섭취량(g) × 펫 체중(kg)**
예: 초콜릿은 카카오 함량(다크/밀크)으로 분기.

### 3.3 예방접종 관리 (담당: 김찬영)

```
[접종 이력 입력] ── 백신 종류 + 접종 날짜 + 차수
        │
        ▼
   다음 권장일 자동 계산 → 알림 예약
        │
        ├─ 강아지: 6주~16주 6차 + 1~3년 부스터
        ├─ 고양이: 8주~16주 3차 + 매년 부스터
        └─ 광견병: 생후 3개월 이상 의무 (가축전염병예방법 §60, 과태료 500만원)
        │
        ▼
   미접종 백신 발생 시 Risk Engine 재계산
```

**위험도 계층**: 법적 의무(광견병) > 고위험 백신 > 저위험 백신 — UI에서 시각 분리

### 3.4 증상 입력 (담당: 김기연)

```
[증상 입력]
   ├─ 체크박스 다중 선택 (구토, 설사, 무기력, 식욕부진 …)
   └─ 자유 입력 (500자 이내, 타임라인 노출용)
        │
        ▼
   저장 → 이벤트 발행
        │
        ▼
┌─────────────────────────────────────────┐
│ 결과 카드 (A + B + C 조합)              │
├─────────────────────────────────────────┤
│ A. 위험도 점수 즉시 갱신                │
│ B. 최근 7일 음식·체중·접종 이벤트 묶음   │
│ C. 정적 권고 문구 (SymptomGuide 마스터) │
│    "구토 24h 지속 시 병원 권장" 등      │
│ ⚠ 본 정보는 참고용, 진단 아님           │
└─────────────────────────────────────────┘
```

**AI 진단 안 함** — 증상→질환 매핑은 의료 책임 영역이라 범위 밖.

### 3.5 Risk Engine 재계산 (담당: 장윤서)

```
이벤트 수신
  │
  ▼
가중치 합산
  ├─ 최근 7일 위험음식 섭취 × 0.35
  ├─ 체중 급변(±10%)        × 0.25
  ├─ 미접종 의무 백신       × 0.20
  └─ 증상 입력 강도         × 0.20
  │
  ▼
총점 0~100 → 등급 분류
  안심(0-30) / 관찰(31-60) / 주의(61-80) / 위험(81-100)
  │
  ├─ 등급 변경 ─→ 푸시 알림 발송
  └─ 등급 동일 ─→ 알림 X (소음 방지)
  │
  ▼
RiskScore 테이블 저장 → 타임라인에 기록
```

가중치는 환경변수/DB 설정으로 외부화 → 코드 수정 없이 튜닝.

---

## 4. 통합 타임라인 / 대시보드 (담당: 김찬영)

```
[펫 대시보드]
   ├─ 위험도 점수 카드 (현재 등급 + 추세)
   ├─ 체중 차트
   ├─ 다음 접종 일정
   └─ 통합 타임라인
         │
         ▼
   3/01  💉 종합백신 완료
   3/15  ⚖  4.2kg (정상)
   3/20  ⚠  포도 소량 섭취
   3/22  ⚖  3.9kg (-0.3kg)
   3/23  🔔 "포도 섭취 후 체중 감소, 증상 확인 권장"
```

모든 이벤트를 시간축 단일 뷰로, 인과 의심 라인은 시각적으로 연결.

---

## 5. 운영 정책 (모든 화면 공통)

| 영역 | 정책 |
|---|---|
| 인증 | JWT Access 15분 + Refresh 14일 (Rotation + 재사용 감지) |
| 인가 | 모든 펫 라우트에 `requireAuth` → `requirePetOwnership` 미들웨어 |
| 응답 코드 | 권한 없음/없는 자원 모두 **404** (enumeration 방지) |
| 면책 고지 | 모든 의료 관련 결과 카드 하단에 "참고용·진단 아님" 상시 노출 |
| Rate Limit | 로그인 IP당 1분 5회 실패 → 5분 잠금 |
| 의료 자료 | 1차 출처(KASA·AVMA·ASPCA·검역본부) + 2개 이상 교차 검증한 시드만 사용 |

---

## 6. 범위 밖 (1차 미구현)

- 동물병원·보호소 찾기 (지도 연동·집주소 수집 회피)
- 다국어 지원
- 증상 → AI 질환 진단
- MFA / 이메일 인증 (회의 결정 시 1차 추가 가능)
- 포인트 보상 시스템

---

> 화면 와이어프레임은 모든 기능 구현 후 Figma에서 별도 작성 (PLAN.md 12절 #7)
