# 사용자 워크플로우 (반려동물 헬스케어)

> 출처: `PLAN.md` (시스템 설계 정본) + `function.md` (담당 정본) + `plan3.md` (워크플로 정본) + `research_taehyun.md` (인증) + `research_chanyoung.md` (백신) + `research_yunseo.md` (위험 음식)
> 시점: 2026-05-11 / MVP 1차 범위 기준

---

## 0. 전체 사용 시나리오 (Bird's-eye view)

```
[가입·로그인] → [반려동물 등록] → [사진 업로드 / 일상 입력]
                                          │
                                          ├─ 성분표 OCR ─► 위험 성분·알러지 경고
                                          ├─ 영수증 OCR ─► 예방접종 자동 기록
                                          ├─ 체중 입력   ─► 급변 알림
                                          └─ 수동 접종 입력
                                          │
                                          ▼
                          [통합 타임라인 / 대시보드]
                                          │
                                          ▼
                          [챗봇] — 누적 기록을 컨텍스트로 자연어 질의응답
```

핵심: **사진 한 장으로 끝나는 일상 기록 → 타임라인 기반 챗봇 상담**.
사용자는 분리된 폼을 일일이 채울 필요 없이, 영수증·성분표 사진만 올리면 자동 분류·매칭·기록된다.

---

## 1. 가입 → 첫 로그인 *(담당: 김태현)*

```
[랜딩 화면]
   │
   ├─ "회원가입"
   │     ├─ 이메일 입력 (RFC 5322 검증, 소문자 정규화)
   │     ├─ 비밀번호 입력 (최소 10자, 차단 목록 + Pwned Passwords 검사)
   │     ├─ 중복 검사
   │     ├─ Argon2id 해시 → DB 저장
   │     └─ 자동 로그인 X → 로그인 화면으로 이동
   │
   └─ "로그인"
         ├─ 이메일 + 비밀번호 입력
         ├─ 사용자 없어도 더미 해시 비교 (타이밍 공격 방어)
         ├─ 검증 성공 → Access(15분) + Refresh(14일) 발급
         └─ httpOnly + Secure + SameSite=Strict 쿠키로 전달
              │
              ▼
         [홈 / 펫 선택]
```

**비밀번호 분실 시**
1. "비밀번호 찾기" → 이메일 입력 (응답은 가입 여부와 무관하게 동일)
2. CSPRNG 토큰 메일 발송 (DB에는 SHA-256 해시만, 30분 1회용)
3. 새 비밀번호 입력 → 모든 활성 세션·Refresh 무효화 → 완료 알림 메일

---

## 2. 반려동물 등록 *(담당: 김태현)*

```
[홈 / 펫 선택] ── 처음이면 자동으로 등록 화면
   │
   ▼
[반려동물 등록]
   ├─ 이름 (필수)
   ├─ 종 (강아지/고양이)
   ├─ 품종 (자유 입력 + 자동완성)
   ├─ 생년월일 (모르면 "대략 N개월" 토글)
   ├─ 성별 / 중성화 여부
   ├─ 현재 체중 (선택)
   └─ 알러지 목록 (예: 닭고기, 감자, 소고기 …)  ★ 4.2 성분표 매칭에 사용
         │
         ▼
   [펫 대시보드로 이동]
```

- 한 계정에 **다수 펫** 등록 가능, 헤더 드롭다운으로 전환
- 등록 후 모든 기록은 `pet_id`로 분리 저장, 모든 API에서 사용자-펫 권한 검증 강제

---

## 3. 일상 데이터 입력

### 3.1 체중 입력 *(담당: 김기연)*

```
[체중 입력] ── 날짜(기본값: 입력 시점) + 몸무게(kg)
        │
        ▼
   저장
        │
        ▼
   ┌─────────────────────────────────────────┐
   │ 자동 처리                                │
   │  • 직전 기록 대비 ±10% 이상 → 급변 플래그 │
   │  • 표준 체중표 비교 → 정상/과/저체중 판정 │
   └─────────────┬───────────────────────────┘
                 ▼
         급변 시 → 푸시 알림 발송
         정상 추세 → 알림 없음 (소음 방지)
                 │
                 ▼
         타임라인에 이벤트 기록
```

### 3.2 사진 업로드 → OCR 자동 분기 *(담당: 장윤서)*

```
[사진 업로드] — 영수증 또는 성분표
        │
        ▼
   [OCRService: 이미지 → 텍스트]
        │
        ▼
   [문서 분류: 영수증 vs 성분표]
   ├─ 영수증 키워드: "결제", "원", "동물병원", 접종 항목명
   └─ 성분표 키워드: "원재료", "성분", "조단백질"
        │
        │   (사용자 확인 UI에서 분류 재확정 가능)
        │
        ├─ 성분표 ─► 3.2.1
        └─ 영수증 ─► 3.2.2
```

원본 이미지·텍스트는 `IngredientScan` 또는 `ReceiptRecord`에 저장. **자동 저장이 아닌 사용자 확정 후 저장** — OCR 오인식으로 인한 잘못된 의료 기록 방지.

#### 3.2.1 성분표 → 위험 성분 & 알러지 매칭 *(담당: 장윤서)*

```
OCR로 추출한 성분 텍스트
   │
   ├─ DangerFood DB 키워드 매칭 (ASPCA 23개)
   │   └─ 일치 시 "이 사료에 [초콜릿] 포함 — 모든 강아지에 위험" 경고
   │
   └─ Pet.allergies 매칭(펫 알러지)
       └─ 일치 시 "이 펫의 [닭고기] 알러지에 해당" 맞춤 경고
   │
   ▼
┌────────────────────────────────────────────┐
│ 성분표 분석 결과                            │
├────────────────────────────────────────────┤
│ ⚠ 위험 성분 1건                             │
│  • 양파 분말 — 적혈구 손상 가능 (ASPCA)   │
├────────────────────────────────────────────┤
│ ⚠ [몽이]의 알러지 일치 1건                  │
│  • 닭고기 — 등록된 알러지 항목             │
├────────────────────────────────────────────┤
│ ⚠ 본 정보는 참고용이며 진단이 아닙니다.    │
└────────────────────────────────────────────┘
   │
   ▼
타임라인에 OCR 이벤트 기록
```

위험 성분 감지는 **화면 즉시 경고**(푸시 알림 채널은 사용 안 함).

#### 3.2.2 영수증 → 예방접종 자동 기록 *(담당: 김찬영)*

```
OCR로 추출한 영수증 텍스트
   │
   ├─ 결제일 추출
   ├─ 접종 항목 추출 (백신명·차수)
   └─ 주사 종류 식별
   │
   ▼
[VaccinationRecord 후보 생성]
   │
   ▼
사용자 확정 화면 → 저장 (source: ocr)
   │
   ▼
다음 권장일 자동 계산 → 알림 예약
```

### 3.3 예방접종 수동 입력 *(담당: 김찬영)*

```
[접종 이력 입력] ── 백신 종류 + 접종 날짜 + 차수
        │
        ▼
   저장 (source: manual) → 다음 권장일 자동 계산 → 알림 예약
        ├─ 강아지: 6주~16주 6차 + 1~3년 부스터
        ├─ 고양이: 8주~16주 3차 + 매년 부스터
        └─ 광견병: 생후 3개월 이상 의무 (가축전염병예방법 §60, 과태료 500만원)
```

**위험도 계층** (단순 시각 분류):
- 법적 의무(광견병) — 미접종 시 강조 경고 + 처벌 안내
- 고위험 백신 (치사율·위험도 높은 질병)
- 저위험 백신

**알림 트리거** (PLAN 6절):
- 다음 접종일 도래: D-7, D-1, D-day
- 미접종 의무 백신: 권장일 초과 시 강조 톤 알림

---

## 4. 통합 타임라인 / 대시보드 *(담당: 김기연)*

```
[펫 대시보드]
   ├─ 다음 접종 일정 카드
   ├─ 체중 차트
   ├─ 최근 OCR 경고 요약
   └─ 통합 타임라인
         │
         ▼
   3/01  💉 예방접종: 종합백신 완료
   3/15  ⚖  몸무게: 4.2kg (정상)
   3/18  📷 성분표 OCR: "양파 분말" 위험 성분 감지
   3/22  ⚖  몸무게: 3.9kg (-0.3kg, 급변 알림)
   3/25  💉 영수증 OCR: 광견병 접종 자동 기록
```

- **카테고리 필터**: 체중 / 음식(OCR) / 접종 / 기타
- **날짜 범위 필터**
- 시간 근접 이벤트(예: 성분표 경고 → 며칠 후 체중 감소)는 시각적으로 연결

---

## 5. 챗봇 상담 *(담당: 김태현)*

```
[챗봇 화면]
   │
   사용자 질문 (자연어, 예: "최근 한 달 우리 몽이 컨디션 어땠어?")
   │
   ▼
[ChatbotService]
   ├─ 해당 펫의 최근 N개 타임라인 이벤트 조회
   ├─ Pet 프로필(종·품종·체중·알러지) 조회
   └─ 시스템 프롬프트 + 컨텍스트 + 질문 조립
   │
   ▼
OpenAI API 호출 → 자연어 응답
   │
   ▼
ChatLog 저장 + 사용자에게 노출 (면책 고지 동반)
```

**제약**:
- AI 진단 금지 — 시스템 프롬프트에 "진단·처방 X, 일반 정보 제공만" 명시
- 면책 고지 항상 노출 ("응급 시 동물병원 방문 권장")
- 응답 컨텍스트는 펫 단위로 격리 (다른 사용자 데이터 누출 차단)

---

## 6. 운영 정책 (모든 화면 공통)

| 영역 | 정책 |
|---|---|
| 인증 | JWT Access 15분 + Refresh 14일 (Rotation + 재사용 감지) |
| 인가 | 모든 펫 라우트에 `requireAuth` → `requirePetOwnership` 미들웨어 |
| 응답 코드 | 권한 없음/없는 자원 모두 **404** (enumeration 방지) |
| 면책 고지 | 모든 의료 관련 결과 카드·챗봇 응답 하단에 "참고용·진단 아님" 상시 노출 |
| Rate Limit | 로그인 IP당 1분 5회 실패 → 5분 잠금 |
| OCR 저장 | 자동 저장 X, 사용자 확정 단계 통과 후 저장 |
| 의료 자료 | 1차 출처(KASA·AVMA·ASPCA·검역본부) + 2개 이상 교차 검증한 시드만 사용 |
| AI 라벨 | OpenAI 응답에 "AI 추정" 라벨 + 1차 소스 참고 안내 |

---

## 7. 범위 밖 (1차 미구현)

- **위험도 점수 엔진** — 음식·체중·접종 가중치 종합 점수·등급 시스템 (MVP 이후 재검토)
- **증상 입력 기능** — 의료 진단 책임 회피
- **AI 진단** — 증상 → 질환 매핑은 의료 책임 영역
- **동물병원·보호소 찾기** — 지도 연동·집주소 수집 회피
- **다국어 지원** — 한국어 단일 운영
- **MFA / 이메일 인증** — 회의 결정 시 1차 추가 가능
- **포인트 보상 시스템**

---

> 화면 와이어프레임은 모든 기능 구현 후 Figma에서 별도 작성 (PLAN.md 12절 #8)
